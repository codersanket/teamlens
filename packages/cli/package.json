{
  "name": "@codememory/cli",
  "version": "0.1.0",
  "description": "CLI — init, watch, search, status commands",
  "type": "module",
  "bin": {
    "codememory": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@codememory/core": "workspace:*",
    "@codememory/mcp-server": "workspace:*",
    "commander": "^13.0.0",
    "chalk": "^5.4.0",
    "chokidar": "^4.0.0",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
